/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Main.java to edit this template
 */
package arrayinitializationexample;

/**
 *
 * @author Adrian
 */
public class ArrayInitializationExample {
    public static void main(String[] args) {
        /*
        // Uso del método para inicializar un array
        int[] numerosConsecutivos = arrayConNumerosConsecutivos(5);
        imprimirArray(numerosConsecutivos);

        // Inicialización directa
        String[] diasSemana = {"Lunes", "Martes", "Miércoles", "Jueves", "Viernes", "Sábado", "Domingo"};
        imprimirArray(diasSemana);

        // Inicialización de un array de objetos
        StringBuilder[] stringBuilderArray = new StringBuilder[3];
        for (int i = 0; i < stringBuilderArray.length; i++) {
        stringBuilderArray[i] = new StringBuilder("cadena " + i);
        }
        imprimirArray(stringBuilderArray);
        }

    static int[] arrayConNumerosConsecutivos(int totalNumeros) {
        int[] r = new int[totalNumeros];
        for (int i = 0; i < totalNumeros; i++) {
        r[i] = i;
        }
        return r;
        }

    static void imprimirArray(Object[] array) {
        for (Object elemento : array) {
        System.out.println(elemento);
        }
        }

    static void imprimirArray(int[] array) {
        for (int elemento : array) {
        System.out.println(elemento);
        }
 *//*
        String[] nombre = new String[5];     
        
                }
        
        static void IntroducirNombre (){
            System.out.println("Introduzca el nombre: ");
            int j = nombre.length;
            for (int i=0; i<j.length;i++)
            suma=suma+j[i];
            */
        }
        
        
        
        
        
        
    }
